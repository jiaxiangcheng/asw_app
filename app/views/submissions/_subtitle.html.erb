<tr>
  <td colspan="2"></td>
  <td class="subtext">
    <span class="score" id="score_<%= submission.id %>"><%= submission.cached_votes_score %> points</span>
    by
    <%= link_to submission.user.name, submission.user, class: :hnuser %>
    <span class="age">
      <%= link_to time_ago_in_words(submission.created_at) + " ago", submission %>
    </span>
    <span id="unv_<%= submission.id %>"></span>
    |
    <a href="#" >hide</a>
    |
    <%= link_to 'web', "https://www.google.com/search?q=#{CGI.escape submission.title}" %>
    |
    <a href="#" >favourite</a>
    |
    <% if submission.comments.count == 0 %>
      <%= link_to "discuss", submission %>
    <% else %>
      <%= link_to pluralize(submission.comments.count, 'comment'), submission %>
    <% end %>
    <% if submission.user == current_user %>
      |
      <%= link_to 'Destroy', submission_path(submission, goto: root_path), method: :delete, data: { confirm: 'Are you sure?'} %>
    <% end %>
  </td>
</tr>
